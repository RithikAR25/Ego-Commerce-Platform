Build sequence (revised with all integrations)
PhaseWhat you buildKey decisions locked in1Docker Compose (MySQL + Redis + ES), Flyway migrations, common layerSchema, response envelope, exception hierarchy2Auth module — register, login, JWT, refresh, rate limitToken lifetimes, BCrypt strength, Redis revocation3Catalog module — products, attribute types/values, variants, Cloudinary uploadSKU format, image folder structure4Elasticsearch index, sync listener, nightly reindex jobIndex mapping, field analyzers5Cart (Redis), inventory reserve/release/commitLock TTL, reservation expiry6Order module — placement, state machine, status historyState transition rules7Razorpay — order creation, webhook handler, idempotencyWebhook secret, failure path8SendGrid — event listeners, templates, notification logTemplate IDs, from-address9Reviews (purchase gate), Wishlist, CouponsEligibility window, coupon stacking policy10Returns + Razorpay refund APIReturn window, refund eligibility11Admin module — dashboard stats, order management, product CRUDAudit log integration12Frontend — storefront (variant selector, search UI, checkout, Razorpay modal)React Query keys, Zod schemas13Frontend — admin dashboardRole guard, protected routes14CI/CD — GitHub Actions, multi-stage Docker build, VPS deploySecrets strategy, health check15Hardening — security headers, input sanitisation, load testNginx config, actuator lockdown

This is the complete, production-grade blueprint. Every decision flows from your technology choices. Ready to generate code for any specific module — just say which one to start with.
